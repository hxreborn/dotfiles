# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db

# Editor swap/backup
*.swp
*.swo
*.tmp
*.bak
*~
.nfs*

# Archives
*.zip
*.tar.gz
*.rar

# Local overrides (never tracked)
*.local
*.local.*

# AI agent metadata
CLAUDE.md
AGENTS.md

# Repo-level generated files
/.config/
/.local/

# OmniWM runtime state (written by app on launch)
omniwm/.config/omniwm/runtime-state.json

# OmniWM v4 legacy config (v5 uses settings.toml)
omniwm/.config/omniwm/settings.json
